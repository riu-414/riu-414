name: Update profile

on:
  schedule: # 03:00 JST
    - cron: "0 18 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Update works and blog
        id: readme
        continue-on-error: true
        run: node scripts/update-readme.mjs

      - name: Generate 3D contributions
        uses: yoshi389111/github-profile-3d-contrib@latest
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}
          SETTING_JSON: profile-3d-settings.json

      - name: Commit and push
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A README.md assets/works profile-3d-contrib
          if git commit -m "chore: update profile"; then
            git push
          fi

      - name: Report skipped updates
        if: steps.readme.outcome == 'failure'
        run: |
          echo "::error::作品またはブログの取得に失敗しました（前回の内容のまま）。ログを確認してください。"
          exit 1
